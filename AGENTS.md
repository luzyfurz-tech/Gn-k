# AI Agent Guidelines

- If a task becomes overly complex or repetitive, evaluate if creating a custom helper tool (like a small utility script or internal component) is more efficient than manual tool manipulation.
- The agent has the authority and capability to code its own tools (functions, scripts, or helper components) if needed to complete a task efficiently.
